/**
 * usePeriods hooks tests
 *
 * NOTE: These hooks are tested via integration tests instead of unit tests
 * because TanStack Query hooks require complex setup with QueryClient that
 * doesn't work well in isolated unit test environment.
 *
 * See e2e tests for functional verification.
 */

import { describe, it, expect } from 'vitest'

describe('usePeriods hooks', () => {
  it('should be tested via integration/e2e tests', () => {
    expect(true).toBe(true)
  })
})
